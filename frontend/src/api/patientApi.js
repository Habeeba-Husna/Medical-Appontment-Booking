// import axiosInstance from './axiosInstance';

// export const fetchUpdatedProfile = async () => {
//     try {
//       const response = await axiosInstance.get('/patient/profile', { withCredentials: true });
//       // Assuming response.data contains the updated profile data
//       setUser(response.data);  // Assuming you store the profile data in state using setUser
//     } catch (error) {
//       console.error('Failed to fetch updated profile', error);
//     }
//   };
  
